# Example environment variables for RanScanAI DB connections
# Do NOT store real passwords in this file. Use a secure secrets manager.

# Either provide a DATABASE_URL, example format:
# DATABASE_URL=postgresql://ranscan_app:APP_PASSWORD@ranscanai-server.postgres.database.azure.com:5432/ranscanai?sslmode=require

# Or provide the individual components:
POSTGRES_USER=ranscan_app
POSTGRES_PASSWORD=your_app_password_here
DB_HOST=ranscanai-server.postgres.database.azure.com
DB_PORT=5432
POSTGRES_DB=ranscanai

# Optional (for azure script automation):
# AZ_PG_ADMIN_PW=your_admin_password_here
# AZ_PG_APP_PW=your_app_password_here
